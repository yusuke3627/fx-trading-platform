# レンジ端の反転（H6）の事前登録

- 日付: 2026-09-17
- 対象: 仮説 H6「横ばいの 1h レンジの端で失敗した抜けは、レンジ中央へ戻る」。intraday 戦略 `range_edge_reversal`（issue #157 の候補 1）
- 判定: **H6 棄却。** 腕 A の約定あたり純損益の CI90 が [−32.23, −1.76] で上限が 0 を下回った（約定 222 件）。事前登録した主判定の規則をそのまま適用したもので、閾値・手順・パラメータは測定後に変えていない。本ノートは測定前の事前登録として書き始め、「結果」以降を測定後に追記した
- 関連: issue #157（候補 1〜3）、issue #153 / [ADR-037](../adr/ADR-037-backtest-account-level-loss-halts.md)（研究リプレイの口座水準の損失停止）、issue #154（到達範囲）、issue #164（block bootstrap）、[ADR-036](../adr/ADR-036-horizon-exit.md)（時間切れ決済）、[ADR-038](../adr/ADR-038-strategy-take-profit.md)（利確距離）、研究ノート [`2026-09-16-h4-failed-spike-reversal-base-edge.md`](2026-09-16-h4-failed-spike-reversal-base-edge.md)、[`2026-09-10-h5-macro-confirmation-ablation.md`](2026-09-10-h5-macro-confirmation-ablation.md)

## 背景

issue #157 は Meme/壇上の記事の着想を 3 候補に具体化し、候補 1「レンジ端の反転」を最初の検証対象にした。上位足で取引する価格帯を決め、中央では待ち、端に来てから下位足で反転を確認する。損切りの根拠になる価格（反転極値）と、エントリーを検討する価格帯（端から 20%）を分ける。

先行する 2 つの測定は、いずれも決済の構造が成績を決めていた。H5（`post_event_failed_breakout`、[`2026-09-10-h5-macro-confirmation-ablation.md`](2026-09-10-h5-macro-confirmation-ablation.md)）では決済 239 件中 236 件が protection fill で、損切りにも利確にも当たらない建玉が銘柄あたり 1 本の枠を占め続け、25 か月中 13 か月で新規建玉が通らなかった。H4（`failed_spike_reversal`、[`2026-09-16-h4-failed-spike-reversal-base-edge.md`](2026-09-16-h4-failed-spike-reversal-base-edge.md)）は時間切れ決済を入れて期間全体を測れたが、8,798 件の粗損益 +0.164 pips に対して執行コスト 0.979 pips で棄却された。どちらも利確を持たず、出口は損切りか（H4 では）時間切れだった。

H6 では、[ADR-038](../adr/ADR-038-strategy-take-profit.md) で使えるようになった broker 側の利確と、[ADR-036](../adr/ADR-036-horizon-exit.md) の時間切れ決済を最初から入れ、損切り・利確・時間切れの決済内訳と、利確・時間切れ・エントリー帯のそれぞれの寄与を測る。issue #157 が「測定前に確定する項目」として挙げた点（セッションの扱いと DST、移動平均と閾値、「一度割る→戻る」の対象足、quote と RR、保有期限とロールオーバーの優先順位、判定規則）をここで確定する。結果を見てから変えた案は、新しい実験として区別する。

## 仮説

**H6（主仮説）**: 1h の EMA(20) の傾きが小さくレンジ幅が十分な局面で、直近 24 本の 1h 足の高値・安値をレンジとし、5m 足がレンジ端を一度割って終値でレンジ内へ戻った直後に端の側から入り、レンジ中央で利確・反転極値の外側で損切り・60 分で時間切れとすると、約定あたり純損益（執行コスト込み）は正である。

**副仮説（要素の寄与）**: 利確、時間切れ決済、エントリー帯（端から 20%）のそれぞれは、無い場合より約定あたり純損益を改善する。

## 事前登録した売買ルール

戦略 `range_edge_reversal`（`strategy_version` 0.1.0、horizon INTRADAY、status RESEARCH_ONLY、USDJPY、時間足は regime 1h / entry 5m）。数値は `config/base.yaml` の `strategies.range_edge_reversal.parameters` に置き、以下ではその名前で参照する。live の overlay（`shadow` / `micro_live` / `production`）には載せない。

### 1. セッションとレンジ

- セッションは `src/trading/indicators/session.py` の窓をそのまま使う。Tokyo 09:00–18:00 Asia/Tokyo、London 08:00–17:00 Europe/London、New York 08:00–17:00 America/New_York。IANA timezone で判定するので DST は自動で追従し、broker のサーバー時刻には依存しない（ADR-024）。
- レンジは「いずれかのセッションが開始した時点」で確定し、次のセッション開始まで固定する。開いているセッションのうち最も遅く開始したものがレンジの主で、London と New York が重なる時間帯に New York が開始したら New York 開始で作り直し、それ以前のレンジは失効する。どのセッションも開いていない時間帯（New York 終了から Tokyo 開始まで）にレンジは無い。
- 「開始時点」は、そのセッションが開いてから strategy が最初に評価した市場イベントの時点とする。研究リプレイは tick ごとに評価するので、開始から数秒以内である。その時点に見える（`known_at` が評価時刻以下の）直近 `range_lookback_bars`（24）本の 1h 足の最高値・最安値がレンジで、H = 高値 − 安値、中央 = (高値 + 安値) / 2。
- 24 本の最古の足の確定観測時刻（`known_at`）がセッション開始の `range_stale_hours`（72）時間より前なら欠測とみなし、そのセッションは対象外にする。24 本が揃わない場合も対象外。
- レンジ確定後に 1h 足がレンジ外（終値が高値より上、または安値より下）で確定したら、そのレンジでの新規建玉を終える（`range_invalidated`）。次のセッション開始で作り直すまで新規は出さない。終値が端ちょうど、またはヒゲだけが外側の足では失効しない。

### 2. レジーム（横ばい）判定

レンジ確定と同じ時点で判定し、セッション中は固定する。

- 傾き: 1h の EMA(`ema_period` = 20) の現在値と `range_slope_lookback`（6）本前の値の差の絶対値が、ATR(`atr_period` = 14, 1h) × `range_slope_max_atr`（0.5）以下。
- 幅: H が ATR(14, 1h) × `range_width_min_atr`（1.5）以上。
- どちらかを満たさなければそのセッションは対象外。

EMA の定義を固定する。直近 26 本（`ema_period + range_slope_lookback`）の 1h 終値に対し、最初の 20 本の単純平均を初期値として `trading.indicators.ema.ema_series` で計算する。「6 本前の値」はこの初期値に等しい。ATR(14, 1h) は `IndicatorService.atr`（Wilder 法、直近最大 200 本）で、レンジ確定時点の値を使う。

### 3. トリガー

- 買い: 5m 確定足の安値がレンジ下端を割る（breach）。その足を含めて `reentry_max_bars`（6）本以内に 5m 終値がレンジ内（下端以上・上端以下）へ戻ったら成立。成立足の直前から遡って「終値 < 下端」が連続する本数を run とし、run = 0 なら成立足自身の安値が下端を割っていること（breach と戻りが同じ足）、run ≥ 1 なら run 本前が breach 足である。run + 1 が 6 を超える戻り（breach から 7 本目以降）は不成立。反転極値 = breach 足から成立足までの最安値。
- 「6 本以内」の定義: 規則は**観測した 5m 確定足の本数**で数え、壁時計の 30 分ではない。`BarBuilder` は tick の無い区間の空足を補完しないので、流動性の薄い時間帯では 6 本が暦の 30 分を超えることがある。30 分は通常時の目安にすぎず、再現条件としては本数の側を正とする。breach 足はレンジ確定後に確定した足に限る。
- 成立時の約定予定価格は最新 quote で、買いは ask、売りは bid。買いは ask が 下端 + `entry_band_fraction`（0.2）× H 以下であること。満たさなければ見送る。
- 売りは上下対称（高値が上端を上抜けて終値が戻る。bid が 上端 − 0.2 × H 以上）。同じ足で両方が成立し得る形では買いを先に評価する。
- スプレッド gate は scalp と同じ `SpreadGate.from_params`（`spread_gate.max_spread_to_atr` 0.5 × ATR(14, 5m)、USDJPY の `absolute_max_spread_pips` 1.5）。通らなければ見送る。
- 同じレンジ・同じ方向は 1 回だけ。「1 回」は signal 生成 1 回で、`setup_id` にレンジの識別子（セッションと開始時刻）を入れ、方向ごとに 1 つ記憶する。帯や RR で見送った試行は記憶を消費せず、同じレンジ内で改めて breach → 戻りが起きれば再評価する。Risk や執行で拒否された signal は再試行しない（既存戦略と同じ）。その帰結として、Risk が拒否した setup（建玉上限 `MAX_OPEN_POSITIONS_PER_SYMBOL` など）はそのレンジ・その方向では二度と出ず、その機会は失われる。H4 / H5 では建玉上限の棄却が大量に出ており、同じ構造が効き得る。到達範囲（`coverage`）と棄却コードを読むときはこの前提に立つ。

### 4. 損切り・利確・RR

- 買いの損切り = 反転極値 − ATR(14, 5m) × `stop_buffer_atr`（0.25）。売りは 反転極値 + 同じ幅。約定予定価格が損切りより不利な側にあれば（買いで ask ≤ 損切り）見送る。
- 利確 = レンジ中央（セッション中は固定）。`take_profit_enabled` が true のとき、約定予定価格から中央までの距離を 0.1 pip に丸めて `take_profit_distance_pips` に載せ（ADR-038）、Portfolio が処理時の entry 価格から broker 側の利確価格へ戻す。丸めは USDJPY の 1 point 以内で、Portfolio が signal を保留した場合は処理時 quote の変動分だけ中央からずれ得る。研究ではこの差を許容し、絶対価格の固定は要求しない。
- RR: (中央 − ask) ≥ `min_reward_to_risk`（1.5）× (ask − 損切り) を満たさなければ見送る。売りは対称。判定は丸め前の価格差で行い、`take_profit_enabled` が false でも同じ判定を行う（利確を載せないだけ）。
- conviction は固定 0.5、`expected_edge_r` は既定 1。どちらも仮説の一部ではない。研究では USDJPY の数量上限が最小ロット（1,000 通貨）に等しいので sizing に効かない。

### 5. 出口と保有期限

- 出口の優先順位は (1) broker 側の損切り・利確（`ProtectionSpec`）、(2) 時間切れ決済（`horizon_exit_enabled` true、`expected_horizon_seconds` 3600。ADR-036）、(3) セッション閉鎖中に反対向きの setup が成立したときの決済専用 signal（base の既存挙動、ADR-031）。(3) はこの戦略ではセッション外にレンジが無いため実質的に到達しない。セッション閉鎖そのものを契機にした決済は無い。
- 時間切れは、保有を最初に観測した snapshot から 3600 秒以上経った最初の市場イベントで決済専用 signal を出す。約定は執行 latency（normal で 150ms）と次の tick の後になる。
- ロールオーバー回避: 開いているセッションのうち最も遅く終わるものの終了まで `session_end_buffer_seconds`（3600）未満なら新規建玉を出さない（保有と逆向きの setup も signal にしない）。New York 終了 17:00 が broker の日付変更にあたるので、60 分前の新規停止と 60 分の保有期限の組で rollover を跨がない設計とする。ただし終了ちょうど 60 分前の entry は、終了までの約定完了を保証しない。跨いだ建玉は `unpriced_rollovers` に数えられ、判定規則の保留条件で捕捉する。
- 研究リプレイには再起動が無く、scenario `normal` では執行の確率的拒否も無い。Risk の拒否（建玉上限など）は起こり得る。live で期限内の決済を保証すること（再起動後の建玉、拒否時の再送。ADR-036 の残課題）は本測定の範囲外である。

### 6. 通貨強弱

候補 1 では使わない。feature（`ctx.features`）を読まない。

## 事前登録した判定規則

### データと実行条件

| 項目 | 値 |
|---|---|
| 期間 | 2024-08-01T00:00:00+00:00 〜 2026-08-29T00:00:00+00:00（broker ラベル、終端は排他。土曜 00:00 境界） |
| tick | H4 / H5 と同じ保存 tick 集合（H4 の `dataset_hash=385b6bb1378fafa69096702b8eaae294e797341ed0032fafcb71a69483cc8fe5`、98,806,999 本。2026-01-23 から 2026-04-08 までの 74 日欠損を含む）。run の `manifest.json` の値が違えば、その差を結果に記す |
| 環境 | `--env backtest`（ADR-037 により口座水準の損失停止なし）、symbol USDJPY、scenario normal、seed 42 |
| warmup | 戦略の宣言値 13 日 16 時間（`IndicatorService` が読む 200 本の 1h 足 = 200 時間 × 7/5 + 2 日）。レンジと EMA が要求するのは 26 本だが、ATR と EMA は読んだ全履歴で更新されるため、期間の冒頭を定常状態と同じ指標値で始めるには 200 本ぶんを遡って読む必要がある。期間開始の 2024-08-01T00:00 broker は 2024-07-31 21:00 UTC でどのセッションも開いていないので、最初のレンジは Tokyo 開始で作られる |
| 腕 | 4 本を並列に流す（`feature_dataset_hash` を揃えるため）。A = 全規則 / A−利確 = `take_profit_enabled=false` / A−時間切れ = `horizon_exit_enabled=false` / A−帯 = `entry_band_fraction=1.0` |

腕の違いはパラメータ上書きだけで、コード・データ・期間・seed は共通である。

### 主判定（H6）

腕 A の**約定あたり純損益**（執行コスト込み、carry は未値付け）の CI90（bootstrap `BOOTSTRAP_SAMPLES` 回、seed 42、約定を独立で交換可能な標本とみなす i.i.d. 区間）で決める。値は比較 CLI の report が with 側（腕 A）として出す `arm_summary` の区間をそのまま使う。上から順に評価する。

| 条件 | 判定 |
|---|---|
| 腕 A の約定が 50 件未満 | 判定不能（標本不足） |
| CI90 下限 > 0 | H6 支持 |
| CI90 上限 < 0 | H6 棄却 |
| それ以外 | 判定不能（差が検出できない） |

issue #164 の block bootstrap（日単位）の区間は参考値として併記するが、判定には使わない。

### 副判定（要素ごと）

A を「有効側」、A−利確 / A−時間切れ / A−帯をそれぞれ「無効側」として、`ablation_compare.judge` の既存規則をそのまま当てる。上から順に評価する。

| 条件 | 判定 |
|---|---|
| 有効側の平均が高く、差の CI90 下限が 0 より大きい | 有効のまま維持（寄与あり） |
| 無効側の約定数が 2 倍以上で、平均が有効側以上 | 無効にする（絞るだけで質が上がらない） |
| どちらかの腕の約定が 10 件未満 | 判定不能（標本不足） |
| それ以外 | 判定不能（差が検出できない） |

無効側が期末に建玉を残して `verify_comparable` が拒否した場合（H4 の時間切れなし腕と同じ形）は、その対を「評価不能」として記録する。

3 つの対はいずれも比較 CLI で流す。帯の対は比較対象パラメータが真偽値でないため、腕ごとの期待値を `--with-value` / `--without-value` で明示する（issue #178 で追加。省略すれば従来どおり with 側 True / without 側 False を要求する）。CLI が行う検査 ―― `COMPARABLE_FIELDS` の全項目一致、`git_commit` の記録、比較対象以外のパラメータの一致、`open_positions_at_end` と `pending_commands_at_end` が 0、末尾の空白が期間の 10%（`TRAILING_BLACKOUT_MAX_RATIO`）以下 ―― を 1 つでも満たさない対は評価不能とする。

### 保留条件

いずれかの腕で `unpriced_rollovers` > 0 なら、その腕を含む判定を保留する（issue #157 の条件、issue #60）。跨いだ建玉の swap を値付けするまで採否を出さない。

### 報告項目

腕ごとに次を記録する。約定数、純損益、約定あたり期待値と CI90（i.i.d.。block 区間は参考値）、最大ドローダウンと live の高水準ドローダウン停止 3.00% との比較、保有時間、決済内訳（`trades.csv` の `reason` 列で `PROTECTION_CLOSE:STOP_LOSS` / `PROTECTION_CLOSE:TAKE_PROFIT` / `CLOSE` に分ける。`CLOSE` はシステム決済で、run の成果物には signal の reason code が残らないため、時間切れと反転 setup による決済を直接は区別できない。時間切れ決済が有効な腕では、`exit_at − entry_at` が 3600 秒以上の `CLOSE` を時間切れ、未満を反転 setup による決済とみなす近似で分け、近似であることを明記する）、月別の約定分布、到達範囲（`summary.json` の `coverage`）、リスク棄却のコード別内訳。

## 測定手順

VPS で YAML 由来の collector（policy / intervention）を先に流し切り、`fx-macro` だけを止めたうえで（tick / bar / account / shadow のタスクは止めない）、4 腕を並列に流す。長時間の run は RDP を切断すると落ちるため、タスクスケジューラ経由で起動する（H4 と同じ）。

```
python -m trading.backtest.research --env backtest --symbol USDJPY --strategy range_edge_reversal --from 2024-08-01T00:00:00+00:00 --to 2026-08-29T00:00:00+00:00 --seed 42 --out reports/h6_a
python -m trading.backtest.research --env backtest --symbol USDJPY --strategy range_edge_reversal --from 2024-08-01T00:00:00+00:00 --to 2026-08-29T00:00:00+00:00 --seed 42 --param take_profit_enabled=false --out reports/h6_no_tp
python -m trading.backtest.research --env backtest --symbol USDJPY --strategy range_edge_reversal --from 2024-08-01T00:00:00+00:00 --to 2026-08-29T00:00:00+00:00 --seed 42 --param horizon_exit_enabled=false --out reports/h6_no_horizon
python -m trading.backtest.research --env backtest --symbol USDJPY --strategy range_edge_reversal --from 2024-08-01T00:00:00+00:00 --to 2026-08-29T00:00:00+00:00 --seed 42 --param entry_band_fraction=1.0 --out reports/h6_no_band
python -m trading.backtest.ablation_compare --with reports/h6_a/<run_id> --without reports/h6_no_tp/<run_id> --param take_profit_enabled --seed 42
python -m trading.backtest.ablation_compare --with reports/h6_a/<run_id> --without reports/h6_no_horizon/<run_id> --param horizon_exit_enabled --seed 42
python -m trading.backtest.ablation_compare --with reports/h6_a/<run_id> --without reports/h6_no_band/<run_id> --param entry_band_fraction --with-value 0.2 --without-value 1.0 --seed 42
```

`research` は戦略の宣言 warmup ぶんの tick を `--from` の前から読む。頭出しが足りず `ensure_period_covered` が拒否したら、`--warmup-days` で縮めて通さず、保存 tick の範囲を確認する。本番の 2 年より先に 2 週間程度の smoke run で、signal が出ること・決済内訳に利確と時間切れが現れることを確かめてから流す。

## 結果

4 腕を 2026-09-17 02:54 JST に並列で開始し、10:56 JST までに全て正常終了した（`open_positions_at_end` と `pending_commands_at_end` はいずれも 0、`rejected_commands` も 0）。

### 実行条件の記録

| 項目 | 値 |
|---|---|
| `git_commit` | `b278551f30a1a1a1285ea68a7f9bd54134ac3077` |
| `git_dirty` / `git_diff_sha256` | `true` / `5dadf405b37daa0b179e96f7043a15d194a0ab5aa8e13bbc1cbe3cd48e1f9226` |
| `dataset_hash` | `938a54fb33a22cea577e13a76c2f5e0486d568dfe444fd6b58218509ccb88978` |
| `feature_dataset_hash` | `917c81d17f0ee9c3351ae297e8bc121429258045d643056839b979823d44ea9c` |
| `swap_dataset_hash` | `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`（空集合の sha256。swap は 1 件も値付けされていない） |
| `config_sha256` | `ffef35683828389e903eb48a8798694b25e2201f76c54e95e5e9ec8cecc0c90d` |
| `engine_version` / `strategy_version` | 0.8.0 / 0.1.0 |
| `tick_count` / `warmup_days` | 100,358,953 / 13.6667 |
| Python | 3.11.9 |
| run_id | A = `d4eab292-48b0-4e96-ac83-cd014dd86223` / A−利確 = `8daf4b27-e085-4a3f-910d-f07d4a16f562` / A−時間切れ = `47202bde-ed72-4b1a-871d-0c5fe71d4679` / A−帯 = `3fd19f26-9113-4491-83d4-76c62190248c` |

**事前登録と違った点が 2 つある。**

**`dataset_hash` は H4 の値と比較できない。** 事前登録の表には H4 の `dataset_hash=385b6bb1…`（98,806,999 本）と同じ集合を使うと書いたが、実際の値は `938a54fb…` で tick は 100,358,953 本だった。ただし**この 2 つは同じ区間を指していない。** `covered_reconstructed_stream` は `stream_between(symbol, read_from, end)` が返す全 tick で digest を更新するため、`dataset_hash` と `tick_count` は `read_from`（= `--from` − warmup）から `--to` までを対象にする。H4 の `warmup_days` は 2.0175、H6 は 13.6667 で、読み込む区間が 11.65 日長い（H6 の `read_from` は 2024-07-18T08:00、H4 は 2024-07-29T23:35）。この差は約 8 営業日にあたり、読込区間全体の平均密度（100,358,953 ÷ 約 660 営業日 ≒ 15 万本/営業日）から見て 120 万本前後になる。観測された差 1,551,954 本と同じ桁で、期間の前半が平均より密であることも合わせれば説明がつく。

**したがって、保存 tick 集合が H4 の測定時から変わったかどうかは、このハッシュの相違からは判定できない。** 増えたとも同じとも言えない。判定への影響は無く、測定期間（2024-08-01〜2026-08-29）は事前登録どおりで、H6 の判定はその区間の約定に基づく。事前登録が「H4 / H5 と同じ保存 tick 集合」と書いたのは DB の中身についての記述であり、`dataset_hash` はそれを確認する手段にならない（warmup が違えば必ず別の値になる）。

**`git_dirty` が true だった。** ただし内訳は未追跡ファイル 7 件のみで、追跡下のファイルに変更は無い（ログ 3 本と、リポジトリ直下に置かれた MT5 調査用の単体スクリプト 4 本）。いずれも `trading` パッケージの外にありリプレイは読み込まないので、`b278551` をチェックアウトすれば同じコードが再現できる。4 腕は `git_diff_sha256` を含む `COMPARABLE_FIELDS` の全項目一致を比較 CLI が検証しており、腕どうしの比較は有効である。

### 腕ごとの成績

| 腕 | 約定 | 純損益 | 素の損益（仲値） | 執行コスト | 最大DD | 約定あたり期待値 | CI90（i.i.d.） | CI90（block、参考） | 勝率 | 保護決済 | `unpriced_rollovers` |
|---|---|---|---|---|---|---|---|---|---|---|---|
| A（全規則） | 222 | −3,907.59 | −1,766.00 | 2,141.59 | 6,620.81 | −17.6018 | [−32.2272, −1.76459] | [−33.3833, −1.57926] | 25.225% | 153 | 0 |
| A−利確 | 222 | −4,177.66 | −2,028.00 | 2,149.66 | 6,570.71 | −18.8183 | [−33.144, −3.45329] | [−34.0156, −3.39955] | 25.225% | 148 | 0 |
| A−時間切れ | 221 | −5,941.57 | −3,708.00 | 2,233.57 | 8,899.60 | −26.8849 | [−47.2214, −5.24837] | [−47.3519, −5.22086] | 12.217% | 221 | 15 |
| A−帯 | 222 | −3,907.59 | −1,766.00 | 2,141.59 | 6,620.81 | −17.6018 | [−32.2272, −1.76459] | [−33.3833, −1.57926] | 25.225% | 153 | 0 |

初期資金は 100 万。腕 A の最大ドローダウンは 0.662%、A−時間切れでも 0.890% で、live の高水準ドローダウン停止 3.00% には達しない。`carry_total` は全腕 0 で、swap は値付けされていない。

### 決済内訳と勝敗の大きさ

`trades.csv` の `reason` 列を集計した（金額は `net_pnl` + `carry`）。

| 腕 | `PROTECTION_CLOSE:STOP_LOSS` | `PROTECTION_CLOSE:TAKE_PROFIT` | `CLOSE` |
|---|---|---|---|
| A | 148 件 / −12,594.05 | **5 件** / +1,846.22 | 69 件 / +6,840.24 |
| A−利確 | 148 件 / −12,594.05 | 0 件 | 74 件 / +8,416.39 |
| A−時間切れ | 194 件 / −17,940.20 | 27 件 / +11,998.63 | 0 件 |

A の `CLOSE` 69 件は全て保有 3,600 秒以上で、3,600 秒未満は 0 件だった。事前登録に書いた「保有時間による近似で時間切れと反転 setup を分ける」は、この run では分ける対象が無い（反転 setup による決済は発生していない）。

| 腕 | 勝ち | 勝ちの平均 | 負け | 負けの平均 | 保有時間 平均 / 最小 / 最大（秒） |
|---|---|---|---|---|---|
| A | 56 | +174.21 | 166 | −82.31 | 1,740 / 1 / 3,618 |
| A−利確 | 56 | +169.39 | 166 | −82.31 | 1,787 / 1 / 3,618 |
| A−時間切れ | 27 | +444.39 | 194 | −92.48 | 6,719 / 1 / 198,600 |

A と A−利確は、損切りの件数・合計・勝敗の件数・負けの平均まで一致する。利確を外して変わったのは利確で閉じた 5 件だけで、その 5 件は保有期限まで持っても勝ちのまま終わっている。

### 到達範囲とリスク棄却

到達範囲は全腕で同じ。初回約定 2024-08-06T12:10:00.658+09:00、最終約定 2026-08-28T20:25:00.912+09:00、25 か月中 23 か月で約定あり。約定の無い 2026-02 と 2026-03 は、事前登録に記した 74 日の tick 欠損（2026-01-23〜04-08）と一致する。末尾の空白は 0.524 日で `TRAILING_BLACKOUT_MAX_RATIO` の範囲内。

月別の約定数（`entry_at` のブローカーラベル月）は次のとおり。A−帯は A と同一のため省く。

| 月 | A | A−利確 | A−時間切れ |
|---|---|---|---|
| 2024-08 | 6 | 6 | 6 |
| 2024-09 | 4 | 4 | 4 |
| 2024-10 | 9 | 9 | 9 |
| 2024-11 | 11 | 11 | 11 |
| 2024-12 | 6 | 6 | 6 |
| 2025-01 | 9 | 9 | 9 |
| 2025-02 | 9 | 9 | 9 |
| 2025-03 | 14 | 14 | 14 |
| 2025-04 | 11 | 11 | 11 |
| 2025-05 | 12 | 12 | 12 |
| 2025-06 | 7 | 7 | 7 |
| 2025-07 | 15 | 15 | 14 |
| 2025-08 | 11 | 11 | 11 |
| 2025-09 | 9 | 9 | 9 |
| 2025-10 | 9 | 9 | 9 |
| 2025-11 | 17 | 17 | 17 |
| 2025-12 | 10 | 10 | 10 |
| 2026-01 | 8 | 8 | 8 |
| 2026-02 | 0 | 0 | 0 |
| 2026-03 | 0 | 0 | 0 |
| 2026-04 | 7 | 7 | 7 |
| 2026-05 | 12 | 12 | 12 |
| 2026-06 | 10 | 10 | 10 |
| 2026-07 | 5 | 5 | 5 |
| 2026-08 | 11 | 11 | 11 |
| **合計** | **222** | **222** | **221** |

A と A−利確は全ての月で一致する。A−時間切れだけ 2025-07 が 1 件少ないが、これは同月 21 日に建玉上限（`MAX_OPEN_POSITIONS_PER_SYMBOL`）で棄却された 1 件に対応する。時間切れ決済が無いと建玉が残り続けるため次の setup が枠に入れず、事前登録に書いたとおりその機会は失われる。

約定は月 4〜17 件に散らばり、特定の月に偏ってはいない。0 件の 2 か月は tick 欠損によるもので、戦略が機会を見つけられなかったのではない。

腕 A のリスク棄却 37 件の内訳は、イベントリスクによる停止（`EVENT_MODE_ALLOWS_ENTRY`）23 件、最小ロットがリスク許容量を超えたもの（`MINIMUM_BROKER_SIZE_EXCEEDS_RISK`）14 件。A−時間切れだけ 38 件で、2025-07-21 に建玉上限（`MAX_OPEN_POSITIONS_PER_SYMBOL`）との複合棄却が 1 件増える。事前登録に書いたとおり、これらの setup は同じレンジ・同じ方向では二度と出ないため、その機会は失われている。

## 判定

### 主判定: H6 棄却

腕 A の約定は 222 件で標本不足の条件（50 件未満）に当たらない。約定あたり純損益の CI90 は **[−32.2272, −1.76459]** で、**上限が 0 を下回る**。事前登録した表の「CI90 上限 < 0 → H6 棄却」に該当する。参考値の block 区間 [−33.3833, −1.57926] も同じ向きだった。

閾値・手順・パラメータはいずれも測定後に変えていない。

### 副判定

| 対 | 差（A − 無効側） | CI90（i.i.d.） | CI90（block、参考） | 判定 |
|---|---|---|---|---|
| 利確（`take_profit_enabled`） | +1.2165 | [−19.7277, 23.1118] | [−20.6937, 22.9684] | 判定不能（差が検出できない） |
| 時間切れ（`horizon_exit_enabled`） | +9.2832 | [−17.5234, 34.6868] | [−17.9752, 36.519] | **保留** |
| 帯（`entry_band_fraction`） | 0 | [−21.4781, 21.7623] | [−22.1975, 22.1994] | 判定不能（差が検出できない） |

**時間切れの対は保留である。** 無効側の `unpriced_rollovers` が 15 件あり、事前登録の保留条件「いずれかの腕で `unpriced_rollovers` > 0 なら、その腕を含む判定を保留する」に該当する。時間切れ決済を外すと建玉が New York 終了のロールオーバーを跨ぐため、これは設計どおりの帰結である（時間切れ決済はまさに跨がないために入れた仕組みで、60 分前の新規停止と組で働く）。跨いだ建玉の swap を値付けするまで採否を出さない（issue #60）。

**比較 CLI はこの保留条件を判定に組み込んでいない。** 表に `unpriced_rollovers` を表示するだけで、`verdict` 行には「判定不能」と印字する。本ノートの保留は事前登録の規則を人が適用した結果であり、CLI の出力をそのまま読むと誤読する。

## 解釈

### 負けの構造

腕 A の勝ちは 56 件で平均 +174.21、負けは 166 件で平均 −82.31 だった。この比（2.12 倍）で損益が均衡する勝率は 82.31 ÷ (174.21 + 82.31) = **32.1%** である。実際の勝率は 56 ÷ 222 = **25.2%** で、7 ポイント足りない。

損切り幅の 1.5 倍を利確目標に置いた設計なので、勝ちが常に目標に届き負けが常に損切り幅どおりなら均衡点は 40% になる。実際の均衡点がそれより低いのは、**時間切れ決済が負けの側も損切りに届く前に切り上げているため**で、平均損失が損切り幅を下回っている。勝ちが目標に届かないことだけを見て必要勝率が 40% より高いと考えるのは誤りである。それでも 25.2% は 32.1% に届かない。

**執行コストを消しても勝ちに転じない。** 素の損益（仲値ベース）が −1,766 で、そもそも負けている。H4 は素の損益 +0.164 pips を執行コスト 0.979 pips が食い潰す形の棄却で、値動きの読み自体には僅かな優位があった。H6 はそこが違う。執行の改善（スプレッド管理、約定タイミング）では届かない。

### 利確はほとんど成立しなかった

`reason` 列を数えると、2 年間で `PROTECTION_CLOSE:TAKE_PROFIT` は **5 件**（合計 +1,846.22、1 件あたり +369.24）だった。事前登録の smoke run（2026-08-08〜08-22、約定 5 件）で利確が 0 件だったのは異常値ではなく、これが定常状態だった。

中央までの値幅が損切り幅の 1.5 倍以上という条件を課したうえで、60 分の保有期限内にその距離を取り切る動きが、この期間の USD/JPY にはほぼ無かったということになる。

**利確を外しても、その 5 件は勝ちのまま終わる。** A−利確では `CLOSE` が 69 件から 74 件へ増え、合計が 6,840.24 から 8,416.39 へ 1,576.15 増える。増えた 5 件は 1 件あたり +315.23 で、利確で閉じた場合の +369.24 より 1 件あたり約 54 小さい。5 件ぶんの差 270.07 は、2 腕の純損益の差（−3,907.59 と −4,177.66）と一致する。**利確の 2 年間の寄与は、5 件からの 270.07 がすべてである。**

### 帯は測定になっていなかった

A−帯（`entry_band_fraction=1.0`）の成果物は A と 1 バイト単位で同一だった（`equity.json` 50,465,877 バイトを含む）。パラメータは正しく渡っており（`manifest.json` の `param_overrides` が `{"entry_band_fraction": 1.0}`、`resolved_parameters` も 1.0）、約定が 1 件も変わらなかった。

理由は、帯の条件がリスクリワードの条件より常に緩いことにある。レンジ幅を W、レンジ下端からエントリー価格までの距離を x、損切りバッファを b（`stop_buffer_atr` × ATR、常に正）とすると、

- 帯: x ≤ `entry_band_fraction` × W
- リスクリワード: (W/2 − x) ≥ 1.5 × (エントリー価格 − 直近極値 + b)

再入場は端を割ってから戻る動きを対象にするので、直近極値はレンジ下端以下、すなわち (エントリー価格 − 直近極値) ≥ x である。これを代入して解くと **x ≤ 0.2 W − 0.6 b** となり、`entry_band_fraction` = 0.2 のときの帯（x ≤ 0.2 W）より必ず厳しい。帯を 1.0 に広げて候補を増やしても、増えた候補はすべてリスクリワードで落ちる。売りは対称。

**つまりこの 2 つは重複した制約で、`entry_band_fraction` は 0.2 では効いていない。** 事前登録の 3 腕のうち 1 腕は、測定する前から差がゼロと決まっていた。候補 2 を設計するときは、帯とリスクリワードのどちらかを外すか、帯を 0.2 より十分小さくしない限り、この対は測定にならない。

### 時間切れ決済について（保留中の観察）

判定は保留だが、観察された差は記録しておく。時間切れ決済を外すと純損益が −3,907.59 から −5,941.57 へ 2,033.98 悪化し、素の損益も 1,942.00 悪化する。最大ドローダウンは 6,620.81 から 8,899.60 へ広がり、勝率は 25.2% から 12.2% へ下がる。保護決済は 153 件から 221 件へ増える。

決済内訳は大きく変わる。損切りが 148 件から 194 件へ、利確が 5 件から 27 件へ増え、`CLOSE` は 0 件になる。保有時間の最大は 3,618 秒から 198,600 秒（約 55 時間）へ伸び、これが `unpriced_rollovers` 15 件の出どころである。勝ちの平均は +174.21 から +444.39 へ上がる一方、件数が 56 から 27 へ減り、負けの平均も −82.31 から −92.48 へ悪化する。均衡に必要な勝率は 92.48 ÷ (444.39 + 92.48) = 17.2% に下がるが、実際の勝率 12.2% はやはり届かない。

**保有期限を外すと利確は届くようになる**（5 件 → 27 件）。60 分という長さが利確をほぼ無効にしていたことは、この対から読み取れる。ただし同時に損切りも増えて全体は悪化しており、期限を延ばせば良いという結論にはならない。

H4 で時間切れ決済を入れた狙い（損切りまで持ち続ける構造を断つ）は、この方向には働いている。ただし損失を縮めているだけで、利益を生んではいない。区間が 0 を跨ぐため、有意とは言えない。

## まだ測っていないこと

- 保有 1 秒で終わった約定が全腕に存在する（保有時間の最小値）。件数と原因は調べていない。損切り幅が entry 時点のスプレッドに対して狭すぎる可能性があるが、個別の約定を見ていないので断定できない。
- 同じ tick 集合には 74 日の欠損（2026-01-23〜04-08）があり、研究ハーネスは期間途中の欠損を検出しない。H4 と同じく、この 2 か月は評価が走らない。
- CI90 は i.i.d. bootstrap で、同じセッションに固まる約定の自己相関を考慮しない（H4 と同じ限界）。block 区間（issue #164）は参考値にとどめる。
- research seed は 1 組（42）で、執行の感応度は測らない。
- 帯の対は `--with-value` / `--without-value`（issue #178）で流した。ただし上の解釈のとおり、この対は差がゼロになることが構造的に決まっており、比較としては成立していない。
- run の成果物には signal の reason code が残らないので、システム決済 `CLOSE` のうち時間切れと反転 setup の内訳は保有時間による近似になる。
- 4 腕は約定集合そのものが違い得る。件数差をそのまま「フィルターで落とした取引」の成績として読まない（issue #157 の検証方針）。
- レンジ確定を「開始後に最初に評価した市場イベント」とする解釈、EMA の初期値の定義、「6 本」を観測本数とする解釈、利確を距離で載せる経路の丸め・遅延は、いずれも結果を見る前にここで固定したもので、run 後に変えない。
- 未追跡ファイルの中身が再現ハッシュ（`git_diff_sha256`）に入る。今回はリポジトリ直下のログ 3 本がそこに含まれ、4 腕の `manifest.json` 生成は 7 分間に分散していた。その間にログが 1 行でも増えていれば腕ごとにハッシュがずれ、比較 CLI が `COMPARABLE_FIELDS` 不一致で全対を拒否していた。今回は偶然揃っただけで、この構成は次回も通る保証がない。
- 比較 CLI は事前登録の保留条件（`unpriced_rollovers` > 0）を `verdict` に反映しない。表に数値を出すだけなので、印字された判定をそのまま読むと保留すべき対を判定済みと誤読する。
- 保存 tick 集合が H4 / H5 の測定時と同じかどうかを確認していない。`dataset_hash` は warmup を含む読込区間に対する値なので、warmup の異なる run どうしでは比較できない。同一性を確かめるには、測定期間だけを対象にした指紋を別途取るか、`market_ticks` の行を直接数える必要がある。
- live での保証（再起動をまたぐ建玉、拒否時の再送、NY 終了までの約定完了）は本測定の範囲外で、昇格判断とは別に扱う。
- 判定用の run とは別に、動作確認として 2026-08-08〜08-22 の smoke run を流している（約定 5 件）。決済内訳は損切り 3・時間切れ 2 で、レンジ中央の利確は 1 件も成立しなかった。smoke の期間は測定期間の内側にあるため、この観測を理由にパラメータ（`expected_horizon_seconds`、利確の RR 閾値、`entry_band_fraction`）は変更していない。
